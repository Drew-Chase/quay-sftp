# Quay SFTP Library

The quay sftp library is a Rust-based library designed to provide a robust and efficient implementation of the SFTP/FTP/S3 protocol(s). It aims to simplify file transfer and management tasks by offering a user-friendly interface that integrates seamlessly with the underlying SFTP protocol.