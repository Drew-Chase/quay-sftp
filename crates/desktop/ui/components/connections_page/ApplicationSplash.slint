import { Layout } from "../../themes/layout.slint";
import { Theme } from "../../themes/theme.slint";
export component ApplicationSplash {
    width: 26rem;
    VerticalLayout {
        x: 0px;
        y: 0px;
        Text {
            text: "Quay";
            font-size: Layout.text.display;
            height: 75px;
            Rectangle {
                width: 10px;
                height: 10px;
                background: Theme.palette.hero.primary.base;
                border-radius: 100px;
                x: 135px;
                y: 50px;
            }
        }

        Text {
            height: 50px;
            text: "A modern SFTP, FTP, and cloud storage explorer with a built-in shell and code editor — designed for keyboards, made for the dark.";
            font-size: Layout.text.lg;
            color: Theme.palette.ink.ink03;
            wrap: TextWrap.word-wrap;
            letter-spacing: Layout.tracking.wide;
        }
    }
}
